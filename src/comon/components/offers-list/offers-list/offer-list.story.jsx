import { OfferList } from './offer-list.component';

export default {
  title: 'Offers/List',
  component: OfferList,
};

const Template = (args) => <OfferList {...args} />

export const View = Template.bind({});
